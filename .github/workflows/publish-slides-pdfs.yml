# SPDX-FileCopyrightText: 2026 Vitor Mattos
# SPDX-License-Identifier: AGPL-3.0-or-later
name: Publish Slides.com PDFs

on:
  push:
    branches:
      - main
    paths:
      - 'presentations/slides.com/**'
      - 'scripts/archive-slides-thumbnails.php'
      - 'scripts/generate-slides-pdfs.php'
      - 'scripts/slides/decktape-reveal.js'
      - 'src/Presentations/PdfExportPolicy.php'
      - 'src/Presentations/SlidesReleaseMetadata.php'
  workflow_run:
    workflows:
      - Sync Slides.com
    types:
      - completed
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: slides-com-pdf-publish
  cancel-in-progress: true

jobs:
  publish:
    if: >-
      github.event_name != 'workflow_run' ||
      (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: main
          persist-credentials: true

      - uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: '8.3'
          coverage: none

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24'

      - name: Install dependencies
        run: |
          composer install --no-interaction --no-progress --prefer-dist
          npm install --no-save --package-lock=false decktape@3.16.1
          cp scripts/slides/decktape-reveal.js node_modules/decktape/plugins/reveal.js

      - name: Archive presentation thumbnails
        run: php scripts/archive-slides-thumbnails.php

      - name: Restore presentation PDF cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .cache/slides-pdf
          key: slides-pdf-v2-${{ runner.os }}-${{ github.run_id }}
          restore-keys: |
            slides-pdf-v2-${{ runner.os }}-

      - name: Publish archival PDFs and synchronize release portfolio
        env:
          GH_TOKEN: ${{ github.token }}
        run: php scripts/generate-slides-pdfs.php

      - name: Commit synchronized release data
        shell: bash
        run: |
          set -euo pipefail
          git add presentations/slides.com source/_talks source/_talksEn

          if git diff --cached --quiet; then
            echo 'Presentation archive and release manifests are already up to date.'
            exit 0
          fi

          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'content: synchronize presentation release assets'
          git pull --rebase origin main
          git push origin HEAD:main
